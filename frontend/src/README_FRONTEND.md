# Frontend implementation notes

Copilot should generate a Next.js App Router application here.

Priority pages:

1. `/`
2. `/search`
3. `/instruments/[id]`
4. `/watchlist`
5. `/history`

Centralize API calls in `src/lib/api.ts` and shared types in `src/types`.
